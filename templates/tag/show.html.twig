{% extends 'base.html.twig' %}

{% block metatitle %}
    Tag modifié | Super Blog
{% endblock metatitle %}

{% block main %}

    <section class="container my-5">
        <div class="d-flex justify-content-between align-items-center mb-4">

            <article>
                <h2> {{ tag.name }}</h2>
            </article>
        </div>


        <a href="{{ path('tag_update', {id:tag.id}) }}" class="btn btn-primary">Modifier</a>
    </section>


{% endblock main %}