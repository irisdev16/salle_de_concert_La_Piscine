{% extends 'base.html.twig' %}

{% block metatitle %}
    Animateur modifié | Super Blog
{% endblock metatitle %}

{% block main %}

    <section class="container my-5">
        <div class="d-flex justify-content-between align-items-center mb-4">

            <article>
                <h2> {{ animator.lastname }}</h2>
                <h2> {{ animator.firstname }}</h2>
                <h2> {{ animator.phone }}</h2>
                <h2> {{ animator.email }}</h2>
            </article>
        </div>


        <a href="{{ path('animator_update', {id:animator.id}) }}" class="btn btn-primary">Modifier</a>
    </section>


{% endblock main %}